#include "main.h"
#include <stdlib.h>

/**
 * _abs - Entry
 *
 * @c: absolute value is returned
 *
 * Return: return the result of abs
 */

int _abs(int c)
{
	int n = -1;

	/* check if c is negative */
	if (c < 0)
		return (c * n);
	else
		return (c);
	return (0);
}
