#include "main.h"
#include <stdio.h>

/**
 * main - checks the code
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	int i = 0;

	while ()
	{
		i += 1;
		argc--;
	}
	printf("%d\n", i);
	return (0);
}
